meta {
  name: Time E2E 17 - Login Back as Admin
  type: http
  seq: 17
}

post {
  url: {{BASE_URL}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "{{timeAdminUsername}}",
    "password": "{{timeAdminPassword}}"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
