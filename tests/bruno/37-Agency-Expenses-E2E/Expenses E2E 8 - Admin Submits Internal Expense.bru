meta {
  name: Expenses E2E 8 - Admin Submits Internal Expense
  type: http
  seq: 8
}

post {
  url: {{BASE_URL}}/api/agency/expenses/{{expAdminId}}/submit
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.expense.status: eq SUBMITTED
}
