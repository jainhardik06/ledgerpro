meta {
  name: Expenses E2E 30 - Login as Admin for Final Approval
  type: http
  seq: 30
}

post {
  url: {{BASE_URL}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "{{expAdminUsername}}",
    "password": "{{expAdminPassword}}"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
