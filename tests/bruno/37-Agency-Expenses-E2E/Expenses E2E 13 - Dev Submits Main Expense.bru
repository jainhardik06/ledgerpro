meta {
  name: Expenses E2E 13 - Dev Submits Main Expense
  type: http
  seq: 13
}

post {
  url: {{BASE_URL}}/api/agency/expenses/{{expMainId}}/submit
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.expense.status: eq SUBMITTED
}
