meta {
  name: Expenses E2E 37 - Agency B Switch to Agency Mode
  type: http
  seq: 37
}

post {
  url: {{BASE_URL}}/api/settings
  body: json
  auth: none
}

body:json {
  {
    "appMode": "Agency"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
