meta {
  name: Expenses E2E 38 - Agency B Login
  type: http
  seq: 38
}

post {
  url: {{BASE_URL}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "{{expBUsername}}",
    "password": "E2eExpB#2026"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
