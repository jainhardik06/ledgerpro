meta {
  name: Invoices E2E 21 - Dev Submits Expense
  type: http
  seq: 21
}

post {
  url: {{BASE_URL}}/api/agency/expenses/{{expenseId}}/submit
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.expense.status: eq SUBMITTED
}
