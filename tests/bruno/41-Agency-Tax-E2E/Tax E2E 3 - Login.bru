meta {
  name: Tax E2E 3 - Login
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "{{taxAdminUsername}}",
    "password": "{{taxAdminPassword}}"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
