meta {
  name: Reports E2E 62 - Invalid date Is 400
  type: http
  seq: 62
}

get {
  url: {{BASE_URL}}/api/agency/reports/portfolio?from=not-a-date
  body: none
  auth: none
}

assert {
  res.status: eq 400
}

docs {
  §28 — dates are business dates (YYYY-MM-DD); anything else is a 400,
  never a silently dropped filter.
}
