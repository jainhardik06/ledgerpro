meta {
  name: Reports E2E 78 - Agency B Switch to Agency Mode
  type: http
  seq: 78
}

post {
  url: {{BASE_URL}}/api/settings
  body: json
  auth: none
}

body:json {
  {
    "appMode": "Agency"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
